// Adapted from https://github.com/JeffDess/zmk-config/blob/main/layouts/canadian-multilingual-standard.dtsi
#undef CONFIG_ZMK_MACRO_DEFAULT_WAIT_MS
#define CONFIG_ZMK_MACRO_DEFAULT_WAIT_MS 30

#undef CONFIG_ZMK_MACRO_DEFAULT_TAP_MS
#define CONFIG_ZMK_MACRO_DEFAULT_TAP_MS 40

/* French Modern Mappings for Linux ("AZERTY, variante")
 * & Windows ("Français (Standard, AZERTY)") 
 */

#define FR_A    Q           // A
#define FR_Z    W           // Z
#define FR_Q    A           // Q
#define FR_W    Z           // W
#define FR_M    SEMI        // M
#define FR_0    LS(N0)      // 0
#define FR_1    LS(N1)      // 1
#define FR_2    LS(N2)      // 2
#define FR_3    LS(N3)      // 3
#define FR_4    LS(N4)      // 4
#define FR_5    LS(N5)      // 5
#define FR_6    LS(N6)      // 6
#define FR_7    LS(N7)      // 7
#define FR_8    LS(N8)      // 8
#define FR_9    LS(N9)      // 9
#define SLSH    SQT         // /
#define BSLSH   LS(SQT)     // 
#define SQUO    MINUS       // '
#define DQUO    LS(MINUS)   // "
#define APO     N8          // ’
#define DEG     RA(MINUS)   // °
#define LBRAC   RA(T)       // {
#define RBRAC   RA(Y)       // }
#define LBKET   RA(N5)      // [
#define RBKET   RA(N6)      // ]
#define LPAREN  N5          // (
#define RPAREN  N6          // )
#define LFQUO   N9          // «
#define RFQUO   N0          // »
#define QUEST   LS(M)       // ?
#define EXCLM   LS(COMMA)   // !
#define VPIP    RA(L)       // |
#define LETH    RA(NUBS)    // ≤
#define GETH    LS(RA(NUBS))    // ≥
#define EQUA    LS(SLASH)   // =
#define LTH     NUBS        // <
#define GTH     LS(NUBS)    // >
#define DOLL    RA(D)       // $
#define EUR     RA(E)       // €
#define PERC    RA(P)       // %
#define PERT    LS(RA(P))   // ‰
#define AROB    GRAVE       // @
#define ESPR    RA(N4)      // &
#define STR     NUHS        // *
#define EURO    RA(E)       // €
#define FPLUS   RBKT        // +
#define HYPMIN  LBKT        // -
#define UNB_HYP LS(LA(LBKT))    // ‑ (unbreakable)
#define NDASH   LS(LBKT)    // –   
#define MDASH   LS(LA(N8))  // —
#define UNDSC   RA(N8)      // _
#define SHARP   LS(GRAVE)   // #
#define SEMIC   SLASH       // ;
#define CLN     DOT         // :
#define PERD    M           // .
#define COMM    COMMA       // ,
#define NBSPC   RA(SPACE)   // unbreakable space
#define T_DOT   LS(DOT)     // …
#define M_DOT   RA(DOT)     // ·
#define INV_QUEST RA(M)     // ¿
#define INV_EXCL RA(COMMA)  // ¡
#define A_AGRAV N1          // à
#define E_ACUTE N2          // é
#define E_AGRAV N3          // è
#define E_CIRC  N4          // ê
#define U_AGRAV RA(U)       // ù
#define FR_OE   RA(O)       // œ
#define FR_AE   RA(A)       // æ
#define C_CEDIL RA(C)       // ç

/* dead keys (waiting to be combined)
 * CD_ is for Combined diacritic 
 */
#define MONET   RA(F)       // ¤*
#define GREEK   RA(G)       // μ*
#define CD_TILD RA(N)       // ~*
#define CD_ACC_CU RA(N2)    // ´*
#define CD_ACC_CI EQUAL     // ^*
#define CD_ACC_GR RA(N3)    // `*
#define CD_TREMA LS(EQUAL)  // ¨*
#define CD_OGO  LS(RA(V))   //  ̨*
#define CD_CED  RA(V)       //  ̧*
#define CD_RON  LS(RA(MINUS))   // ̊*
#define CD_CAR  RA(EQUAL)   //  ̌*
#define CD_MAC  RA(N7)      //  ̄*
#define CD_BRE  RA(GRAVE)   //  ̆*
#define CD_INV_BRE LS(RA(GRAVE))    //  ̑*
#define CD_UPO  RA(I)       // ̇*
#define CD_LPO  LS(RA(I))   // ̣*
#define CD_ACC_DCU LS(RA(N5))   // ̋*
#define CD_ACC_DGR LS(RA(N6))   // ̏*
#define CD_COM  LS(RA(COMMA))   // ̦*
#define CD_OBL  RA(K)       //  ̷*

/* Symbols */
#define MATH_PM     LS(RBKT)    // ±
#define MATH_DEG    RA(MINUS)   // °
#define MATH_MICRO  LS(NUHS)    // µ
#define MATH_MIN    LA(LBKT)    // −
#define MATH_MUL    RA(NUHS)    // ×
#define MATH_DIV    RA(SQT)     // ÷
#define MATH_SQR    LS(RA(SQT)) // √
#define MATH_DIF    LS(RA(SQT)) // ≠
#define MATH_ASY    RA(SQT)     // ≃
// #define MATH_NOT                // ¬ not present in modern azerty
#define MATH_1_4    LS(RA(NUHS))    // ¼
#define MATH_1_2    LS(NUHS)    // ½
#define MATH_1_2    LS(NUHS)    // ½
// #define MATH_3_4    // ¾ not present in modern azerty


// MOD-MORPH - lowercase / uppercase accented characters
// NOTE: Only accents accessed with a dead key on french keyboard need to have this mod-morph
// Otherwise the shift key will be pressed before the macro and will not be registering
ZMK_MOD_MORPH(a_circ, bindings = <&kp CD_ACC_CI &kp FR_A>, <&kp CD_ACC_CI &kp LS(FR_A)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // â / Â
ZMK_MOD_MORPH(e_trem, bindings = <&kp CD_TREMA &kp E>, <&kp CD_TREMA &kp LS(E)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ë / Ë
ZMK_MOD_MORPH(i_circ, bindings = <&kp CD_ACC_CI &kp I>, <&kp CD_ACC_CI &kp LS(I)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // î / Î
ZMK_MOD_MORPH(i_trem, bindings = <&kp CD_TREMA &kp I>, <&kp CD_TREMA &kp LS(I)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ï / Ï
ZMK_MOD_MORPH(o_circ, bindings = <&kp CD_ACC_CI &kp O>, <&kp CD_ACC_CI &kp LS(O)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ô / Ô
ZMK_MOD_MORPH(o_trem, bindings = <&kp CD_TREMA &kp O>, <&kp CD_TREMA &kp LS(O)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ö / Ö
ZMK_MOD_MORPH(u_circ, bindings = <&kp CD_ACC_CI &kp U>, <&kp CD_ACC_CI &kp LS(U)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // û / Û
ZMK_MOD_MORPH(u_trem, bindings = <&kp CD_TREMA &kp U>, <&kp CD_TREMA &kp LS(U)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ü / Ü

// Accents with a alt+shift needed for uppercase
ZMK_MOD_MORPH(e_cute, bindings = <&kp E_ACUTE>, <&kp RA(E_ACUTE)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // é / É
ZMK_MOD_MORPH(e_grav, bindings = <&kp E_AGRAV>, <&kp RA(E_AGRAV)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // è / È
ZMK_MOD_MORPH(e_circ, bindings = <&kp E_CIRC>, <&kp RA(E_CIRC)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // ê / Ê
ZMK_MOD_MORPH(a_grav, bindings = <&kp A_AGRAV>, <&kp RA(A_AGRAV)>; mods = <(MOD_LSFT|MOD_RSFT)>;)  // à / À
